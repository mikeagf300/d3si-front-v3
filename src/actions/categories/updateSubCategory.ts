/**
 * @deprecated Este archivo ya no es necesario.
 * Usa `updateCategory(id, data)` de "@/actions/categories/updateCategory" en su lugar.
 * El nuevo backend unifica la actualización de categorías y subcategorías en un mismo endpoint.
 */
export {} // archivo mantenido por compatibilidad
